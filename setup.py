from setuptools import setup, find_packages

setup(
    name = 'pytray',
    version = '0.0.3',
    author = '',
    author_email = 'littledon.ram@gmail.com',
    py_modules = ["pytray"],
    packages = find_packages(),
    package_dir = {'' : 'src'}
)